name: Deploy
on: workflow_call   #We use workflow_call to make this workflow accessible in another workflows
jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Output info
              run: echo "Deploying..."