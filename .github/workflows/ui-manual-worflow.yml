name: Manual Workflow
permissions:
  contents: write
  pages: write
on:
    workflow_dispatch:
        inputs:
            test_name:
                description: 'Test name to run'
                default: 'smoke'
                required: true
                type: string
            parallel_workers:
                description: 'Parallel workers'
                required: false
                default: 3
                type: number
            test_run:
                description: 'Test script to run'
                required: true
                default: 'regression'
                type: choice
                options:
                    - regression
                    - letsShop_PlaceOrder
                    - bookAnEvent_test
                    - singleEventWithRefund_test
                    - groupTicketBooking_test
jobs:
    setup_test:
        timeout-minutes: 60
        runs-on: ubuntu-latest
        strategy:
            fail-fast: false
            matrix:
                shard-index: [1,2,3]
                shard-total: [3]
        steps:
            - uses: act10ns/slack@v2
              with:
                status: starting
                channel: '#workflows'
                message: Starting Playwright Test Run...
              env:
                SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

            - name: Code Checkout
              uses: actions/checkout@v6

            - name: Load Cache Deps
              uses: ./.github/actions/cached-deps
              with:
                caching: 'true'
                node-version: ${{ vars.DEFAULT_NODE_VERSION }}

            - name: Run Playwright Tests
              run: npm run ${{ inputs.test_run || 'regression' }} -- --shard=${{ matrix.shard-index }}/${{ matrix.shard-total }}

            - name: Extract test results
              if: ${{ !cancelled() }}
              run: |
                PASSED=$(cat test-results.json | jq '.stats.expected')
                FAILED=$(cat test-results.json | jq '.stats.unexpected')
                SKIPPED=$(cat test-results.json | jq '.stats.skipped')
                TOTAL=$(cat test-results.json | jq '.stats.expected + .stats.unexpected + .stats.skipped')
                echo "passed=$PASSED"   >> $GITHUB_OUTPUT
                echo "failed=$FAILED"   >> $GITHUB_OUTPUT
                echo "skipped=$SKIPPED" >> $GITHUB_OUTPUT
                echo "total=$TOTAL"     >> $GITHUB_OUTPUT
              id: results

            - name: Upload Artifacts
              uses: actions/upload-artifact@v4.6.2
              if: ${{ !cancelled() }}
              with:
                name: all-blob-reports-shard-${{ matrix.shard-index }}
                path: blob-report/
                retention-days: 7

    get-report:
        needs: setup_test
        if: ${{ !cancelled() }}
        uses: ./.github/workflows/reusableWorkflow.yml
        with:
            artifact_name: 'all-blob-reports'
            node-version: ${{ vars.DEFAULT_NODE_VERSION }}
            retentionDays: 30

    notify:
        needs: get-report
        runs-on: ubuntu-latest
        if: ${{ !cancelled() }}
        steps:
            - name: Print report URLs
              run: |
                echo "Allure artifact : ${{ needs.get-report.outputs.allure-report-url }}"
                echo "HTML artifact   : ${{ needs.get-report.outputs.html-report-url }}"
                echo "Live Allure URL : ${{ needs.get-report.outputs.pages-url }}"

            - name: Notify Slack — Completed
              uses: act10ns/slack@v2
              with:
                status: ${{ needs.report.result }}
                channel: '#workflows'
                message: |
                    ${{ needs.report.result == 'success' && '✅' || '❌' }} *Playwright Run #${{ github.run_number }}* — ${{ needs.report.result }}

                    📊 *Test Results:*
                    ✅ Passed  : ${{ needs.report.outputs.passed }}
                    ❌ Failed  : ${{ needs.report.outputs.failed }}
                    ⏭️ Skipped : ${{ needs.report.outputs.skipped }}
                    📝 Total   : ${{ needs.report.outputs.total }}

                    🔗 *Report:* ${{ needs.report.outputs.pages-url }}
                    🌿 *Branch:* ${{ github.ref_name }}
                    👤 *By:* ${{ github.actor }}
              env:
                SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
              if: always()
